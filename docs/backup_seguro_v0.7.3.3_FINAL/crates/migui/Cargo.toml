[package]
name = "migui"
version = "0.4.0"
edition = "2021"
description = "Immediate Mode GUI puro en Rust para RyDit Engine"
authors = ["Shield Project"]

[dependencies]
# Sin dependencias - migui es puro Rust!
